apiVersion: apps/v1
kind: Deployment

metadata:
  name: flaskapp
  namespace: demo

spec:
  replicas: 1

  selector:
    matchLabels:
      app: flaskapp

  template:
    metadata:
      labels:
        app: flaskapp

    spec:
      containers:
      - name: flaskapp

        image: acrdevplatform001.azurecr.io/flaskapp:latest

        ports:
        - containerPort: 5000